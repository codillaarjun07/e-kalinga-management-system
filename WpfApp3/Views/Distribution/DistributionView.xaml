<UserControl x:Class="WpfApp3.Views.Distribution.DistributionView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:vm="clr-namespace:WpfApp3.ViewModels.Distribution" xmlns:conv="clr-namespace:WpfApp3.Views.Allotment" Background="Transparent">

    <UserControl.DataContext>
        <vm:DistributionViewModel />
    </UserControl.DataContext>

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <conv:EqualsToBoolConverter x:Key="EqualsToBoolConverter" />

        <!-- COLORS -->
        <SolidColorBrush x:Key="PageBackgroundBrush" Color="{DynamicResource ThemeColor_Page_F6F8FC}" />
        <SolidColorBrush x:Key="CardBrush" Color="{DynamicResource ThemeColor_Surface_FFFFFF}" />
        <SolidColorBrush x:Key="AccentBrush" Color="{DynamicResource ThemeColor_Accent_1F2A44}" />
        <SolidColorBrush x:Key="AccentHoverBrush" Color="{DynamicResource ThemeColor_AccentHover_4338CA}" />
        <SolidColorBrush x:Key="AccentSoftBrush" Color="{DynamicResource ThemeColor_Accent_EEF2FF}" />
        <SolidColorBrush x:Key="BorderBrushSoft" Color="{DynamicResource ThemeColor_Border_E7ECF5}" />
        <SolidColorBrush x:Key="BorderBrushLighter" Color="{DynamicResource ThemeColor_Border_F1F5F9}" />
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="{DynamicResource ThemeColor_Accent_0F172A}" />
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="{DynamicResource ThemeColor_TextSecondary_64748B}" />
        <SolidColorBrush x:Key="DangerBrush" Color="{DynamicResource ThemeColor_DangerSoft_E11D48}" />
        <SolidColorBrush x:Key="DangerHoverBrush" Color="{DynamicResource ThemeColor_DangerHover_BE123C}" />

        <DropShadowEffect x:Key="CardShadow" Color="{DynamicResource ThemeColor_Shadow_1E293B}" BlurRadius="28" ShadowDepth="0" Opacity="0.10" />

        <!-- TEXT -->
        <Style x:Key="SectionTitleText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource ThemeBrush_AccentText_0F172A}" />
            <Setter Property="FontSize" Value="20" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="FieldLabelText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource ThemeBrush_TextSecondary_64748B}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>

        <!-- GENERIC CARD -->
        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource ThemeBrush_Surface_FFFFFF}" />
            <Setter Property="CornerRadius" Value="20" />
            <Setter Property="Padding" Value="20" />
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeBrush_Border_F1F5F9}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Effect" Value="{StaticResource CardShadow}" />
        </Style>

        <!-- Backdrop -->
        <Style x:Key="BackdropButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="{x:Null}" />
            <Setter Property="Foreground" Value="Transparent" />
            <Setter Property="Cursor" Value="Arrow" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Icon button -->
        <Style x:Key="IconButton" TargetType="Button">
            <Setter Property="Width" Value="42" />
            <Setter Property="Height" Value="42" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="{DynamicResource ThemeBrush_Accent_1F2A44}" />
            <Setter Property="Foreground" Value="{DynamicResource ThemeBrush_TextPrimary_FFFFFF}" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeBrush_AccentHover_4338CA}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.92" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary button -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ThemeBrush_Accent_1F2A44}" />
            <Setter Property="Foreground" Value="{DynamicResource ThemeBrush_TextPrimary_FFFFFF}" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Padding" Value="18,11" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="12" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeBrush_AccentHover_4338CA}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.92" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Light button -->
        <Style x:Key="LightButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ThemeBrush_InfoSoft_F8FAFC}" />
            <Setter Property="Foreground" Value="{DynamicResource ThemeBrush_AccentText_0F172A}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeBrush_Border_E7ECF5}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="16,10" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="12" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeBrush_InfoSoft_F1F5F9}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Pagination -->
        <Style x:Key="PageButton" TargetType="Button">
            <Setter Property="MinWidth" Value="38" />
            <Setter Property="Height" Value="34" />
            <Setter Property="Margin" Value="4,0" />
            <Setter Property="Padding" Value="10,4" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource ThemeBrush_TextSecondary_64748B}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" CornerRadius="10" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeBrush_Accent_EEF2FF}" />
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource ThemeBrush_Accent_EEF2FF}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Input fix -->
        <Style x:Key="InputBorderStyle" TargetType="Border">
            <Setter Property="Height" Value="42" />
            <Setter Property="Padding" Value="14,0" />
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Background" Value="{DynamicResource ThemeBrush_Surface_FFFFFF}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeBrush_Border_E7ECF5}" />
            <Setter Property="BorderThickness" Value="1" />
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource ThemeBrush_Accent_1F2A44}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ReadOnlyInputBorderStyle" TargetType="Border" BasedOn="{StaticResource InputBorderStyle}">
            <Setter Property="Background" Value="{DynamicResource ThemeBrush_InfoSoft_F8FAFC}" />
        </Style>

        <Style x:Key="PlainInputTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="{DynamicResource ThemeBrush_AccentText_0F172A}" />
            <Setter Property="CaretBrush" Value="{DynamicResource ThemeBrush_AccentText_0F172A}" />
            <Setter Property="SelectionBrush" Value="{DynamicResource ThemeBrush_InfoSoft_C7D2FE}" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        </Style>

        <!-- Combo -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource ThemeBrush_AccentText_0F172A}" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="MinHeight" Value="42" />
        </Style>

        <!-- Searchable Project / Allotment picker -->
        <Style x:Key="ProjectSearchTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="{DynamicResource ThemeBrush_AccentText_0F172A}" />
            <Setter Property="CaretBrush" Value="{DynamicResource ThemeBrush_AccentText_1F2A44}" />
            <Setter Property="SelectionBrush" Value="{DynamicResource ThemeBrush_Border_CBD5E1}" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        </Style>

        <Style x:Key="ProjectDropButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="38" />
            <Setter Property="Height" Value="38" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Width="38" Height="38" CornerRadius="10" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeBrush_InfoSoft_EEF2FF}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.85" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ProjectOptionListBoxItem" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0,0,0,4" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" CornerRadius="12" Background="Transparent" Padding="12,10">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeBrush_InfoSoft_F8FAFC}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeBrush_Accent_EEF2FF}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Icon-only classification filter -->
        <Style x:Key="FilterIconComboBox" TargetType="{x:Type ComboBox}">
            <Setter Property="Width" Value="42" />
            <Setter Property="Height" Value="42" />
            <Setter Property="Background" Value="{DynamicResource ThemeBrush_Accent_1F2A44}" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource ThemeBrush_TextPrimary_FFFFFF}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="{x:Type ComboBoxItem}">
                        <Setter Property="Padding" Value="14,9" />
                        <Setter Property="Foreground" Value="{DynamicResource ThemeBrush_AccentText_0F172A}" />
                        <Setter Property="FontSize" Value="13" />
                        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                                    <Border x:Name="Bd" Background="Transparent" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsHighlighted" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeBrush_Accent_EEF2FF}" />
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeBrush_InfoSoft_F8FAFC}" />
                                            <Setter Property="FontWeight" Value="SemiBold" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid>
                            <ToggleButton x:Name="PART_DropDownToggle" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" Foreground="{TemplateBinding Foreground}" Focusable="False" ClickMode="Press" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Bd" Width="42" Height="42" CornerRadius="12" Background="{TemplateBinding Background}">
                                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="" FontSize="15" Foreground="{TemplateBinding Foreground}" VerticalAlignment="Center" HorizontalAlignment="Center" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeBrush_AccentHover_4338CA}" />
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="Bd" Property="Opacity" Value="0.92" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <Popup x:Name="PART_Popup" AllowsTransparency="True" Focusable="False" IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" PlacementTarget="{Binding ElementName=PART_DropDownToggle}" PopupAnimation="Fade">
                                <Border MinWidth="190" Margin="0,8,0,0" Background="{DynamicResource ThemeBrush_Surface_FFFFFF}" BorderBrush="{DynamicResource ThemeBrush_Border_E7ECF5}" BorderThickness="1" CornerRadius="14" Padding="6" Effect="{StaticResource CardShadow}">
                                    <ScrollViewer MaxHeight="260" VerticalScrollBarVisibility="Auto" CanContentScroll="True">
                                        <ItemsPresenter />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Rounded confirm -->
        <Style x:Key="RoundedPrimary" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Width" Value="140" />
            <Setter Property="Height" Value="42" />
            <Setter Property="Padding" Value="0" />
        </Style>

        <!-- Cell alignment -->
        <Style x:Key="CenteredCellStyle" TargetType="DataGridCell">
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Padding" Value="8,0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource ThemeBrush_AccentText_0F172A}" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Foreground" Value="{DynamicResource ThemeBrush_AccentText_0F172A}" />
                    <Setter Property="BorderBrush" Value="Transparent" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CenteredTextBlockStyle" TargetType="TextBlock">
            <Setter Property="Padding" Value="10,0" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        </Style>

        <!-- Close button -->
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="34" />
            <Setter Property="Height" Value="34" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" CornerRadius="10" Background="Transparent">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeBrush_InfoSoft_F1F5F9}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DISTRIBUTION COMMAND CENTER STYLES -->
        <SolidColorBrush x:Key="SuccessBrush" Color="{DynamicResource ThemeColor_SuccessSoft_16A34A}" />
        <SolidColorBrush x:Key="SuccessSoftBrush" Color="{DynamicResource ThemeColor_SuccessSoft_ECFDF3}" />
        <SolidColorBrush x:Key="WarningBrush" Color="{DynamicResource ThemeColor_WarningSoft_D97706}" />
        <SolidColorBrush x:Key="WarningSoftBrush" Color="{DynamicResource ThemeColor_WarningSoft_FFF7E8}" />
        <SolidColorBrush x:Key="InfoBrush" Color="{DynamicResource ThemeColor_InfoSoft_2563EB}" />
        <SolidColorBrush x:Key="InfoSoftBrush" Color="{DynamicResource ThemeColor_InfoSoft_EFF6FF}" />

        <Style x:Key="MetricCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource ThemeBrush_Surface_FFFFFF}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeBrush_Border_F1F5F9}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="18" />
            <Setter Property="Padding" Value="16" />
            <Setter Property="Margin" Value="0,0,12,0" />
            <Setter Property="Effect" Value="{StaticResource CardShadow}" />
        </Style>

        <Style x:Key="SegmentButtonStyle" TargetType="Button">
            <Setter Property="MinWidth" Value="78" />
            <Setter Property="Height" Value="34" />
            <Setter Property="Padding" Value="14,0" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource ThemeBrush_TextSecondary_64748B}" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeBrush_InfoSoft_F1F5F9}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SuccessButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ThemeBrush_SuccessSoft_16A34A}" />
            <Setter Property="Foreground" Value="{DynamicResource ThemeBrush_TextPrimary_FFFFFF}" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Padding" Value="20,12" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="12" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeBrush_Accent_15803D}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.90" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        

        

        
    <Style x:Key="QueueColumnHeaderStyle" TargetType="{x:Type DataGridColumnHeader}"><Setter Property="Height" Value="48" /><Setter Property="MinHeight" Value="48" /><Setter Property="Background" Value="{DynamicResource ThemeBrush_InfoSoft_F8FAFC}" /><Setter Property="Foreground" Value="{DynamicResource ThemeBrush_TextSecondary_64748B}" /><Setter Property="BorderBrush" Value="{DynamicResource ThemeBrush_InfoBorder_E8EEF6}" /><Setter Property="BorderThickness" Value="0,0,0,1" /><Setter Property="Padding" Value="14,0" /><Setter Property="FontSize" Value="11" /><Setter Property="FontWeight" Value="SemiBold" /><Setter Property="HorizontalContentAlignment" Value="Center" /><Setter Property="VerticalContentAlignment" Value="Center" /><Setter Property="Cursor" Value="Hand" /><Setter Property="FocusVisualStyle" Value="{x:Null}" /><Setter Property="SnapsToDevicePixels" Value="True" /><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="{x:Type DataGridColumnHeader}"><Border x:Name="HeaderBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><ContentPresenter Grid.Column="0" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" /><TextBlock x:Name="SortGlyph" Grid.Column="1" Margin="7,0,0,0" FontFamily="Segoe MDL2 Assets" FontSize="9" Foreground="{DynamicResource ThemeBrush_TextSecondary_94A3B8}" VerticalAlignment="Center" Visibility="Collapsed" /></Grid></Border><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="HeaderBorder" Property="Background" Value="{DynamicResource ThemeBrush_InfoSoft_F8FAFC}" /><Setter Property="Foreground" Value="{DynamicResource ThemeBrush_TextPrimary_334155}" /></Trigger><Trigger Property="IsPressed" Value="True"><Setter TargetName="HeaderBorder" Property="Background" Value="{DynamicResource ThemeBrush_InfoSoft_F1F5F9}" /></Trigger><Trigger Property="SortDirection" Value="Ascending"><Setter TargetName="SortGlyph" Property="Text" Value="" /><Setter TargetName="SortGlyph" Property="Visibility" Value="Visible" /></Trigger><Trigger Property="SortDirection" Value="Descending"><Setter TargetName="SortGlyph" Property="Text" Value="" /><Setter TargetName="SortGlyph" Property="Visibility" Value="Visible" /></Trigger><Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.65" /></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter></Style><Style x:Key="QueueLeftColumnHeaderStyle" TargetType="{x:Type DataGridColumnHeader}" BasedOn="{StaticResource QueueColumnHeaderStyle}"><Setter Property="HorizontalContentAlignment" Value="Left" /></Style><Style x:Key="QueueRowStyle" TargetType="DataGridRow"><Setter Property="Background" Value="Transparent" /><Setter Property="BorderBrush" Value="{DynamicResource ThemeBrush_Border_F1F5F9}" /><Setter Property="BorderThickness" Value="0,0,0,1" /><Setter Property="FocusVisualStyle" Value="{x:Null}" /><Setter Property="Cursor" Value="Hand" /><Style.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="{DynamicResource ThemeBrush_InfoSoft_F8FAFF}" /></Trigger><Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="Transparent" /><Setter Property="BorderBrush" Value="{DynamicResource ThemeBrush_Border_F1F5F9}" /></Trigger></Style.Triggers></Style><Style x:Key="QueueCellStyle" TargetType="DataGridCell"><Setter Property="VerticalContentAlignment" Value="Center" /><Setter Property="Padding" Value="12,0" /><Setter Property="BorderThickness" Value="0" /><Setter Property="Background" Value="Transparent" /><Setter Property="Foreground" Value="{DynamicResource ThemeBrush_AccentText_0F172A}" /><Setter Property="FocusVisualStyle" Value="{x:Null}" /><Style.Triggers><Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="Transparent" /><Setter Property="Foreground" Value="{DynamicResource ThemeBrush_AccentText_0F172A}" /><Setter Property="BorderBrush" Value="Transparent" /></Trigger></Style.Triggers></Style></UserControl.Resources>

    <Grid>

        <!-- PAGE -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- ACTIVE ALLOTMENT COMMAND HEADER -->
            <Border Grid.Row="0" CornerRadius="22" Background="{DynamicResource ThemeBrush_Surface_FFFFFF}" BorderBrush="{DynamicResource ThemeBrush_Border_F1F5F9}" BorderThickness="1" Padding="20" Margin="0,0,0,14" Effect="{StaticResource CardShadow}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.35*" />
                        <ColumnDefinition Width="28" />
                        <ColumnDefinition Width="0.9*" />
                        <ColumnDefinition Width="28" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="ACTIVE ALLOTMENT" Foreground="{DynamicResource ThemeBrush_TextSecondary_64748B}" FontSize="11" FontWeight="Bold" Margin="0,0,0,8" />

                        <Grid>
                            <Border x:Name="ProjectSearchHost" Height="48" CornerRadius="14" BorderBrush="{DynamicResource ThemeBrush_Border_E7ECF5}" BorderThickness="1" Background="{DynamicResource ThemeBrush_InfoSoft_FCFDFE}" Cursor="IBeam" PreviewMouseLeftButtonDown="ProjectSearchHost_PreviewMouseLeftButtonDown">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="42" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="42" />
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" Width="30" Height="30" CornerRadius="9" Background="{DynamicResource ThemeBrush_Accent_EEF2FF}" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="" FontSize="13" Foreground="{DynamicResource ThemeBrush_AccentText_1F2A44}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>

                                    <TextBox x:Name="ProjectSearchTextBox" Grid.Column="1" Text="{Binding ProjectSearchText, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ProjectSearchTextBoxStyle}" FontSize="14" FontWeight="SemiBold" ToolTip="Type to search project or allotment" GotKeyboardFocus="ProjectSearchTextBox_GotKeyboardFocus" LostKeyboardFocus="ProjectSearchTextBox_LostKeyboardFocus" PreviewKeyDown="ProjectSearchTextBox_PreviewKeyDown" />

                                    <Button Grid.Column="2" Style="{StaticResource ProjectDropButtonStyle}" ToolTip="Show or hide allotments" Click="ProjectDropButton_Click">
                                        <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="12" Foreground="{DynamicResource ThemeBrush_TextSecondary_64748B}" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsProjectDropdownOpen}" Value="True">
                                                            <Setter Property="Text" Value="" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Button>
                                </Grid>
                            </Border>

                            <Popup x:Name="ProjectPopup" PlacementTarget="{Binding ElementName=ProjectSearchHost}" Placement="Bottom" AllowsTransparency="True" PopupAnimation="Fade" StaysOpen="True" IsOpen="{Binding IsProjectDropdownOpen, Mode=TwoWay}">
                                <Border Width="{Binding ActualWidth, ElementName=ProjectSearchHost}" Margin="0,8,0,0" Background="{DynamicResource ThemeBrush_Surface_FFFFFF}" BorderBrush="{DynamicResource ThemeBrush_Border_E7ECF5}" BorderThickness="1" CornerRadius="16" Padding="6" Effect="{StaticResource CardShadow}">
                                    <ListBox x:Name="ProjectOptionsList" ItemsSource="{Binding FilteredProjects}" BorderThickness="0" Background="Transparent" MaxHeight="260" ScrollViewer.VerticalScrollBarVisibility="Auto" SelectionChanged="ProjectOptionsList_SelectionChanged" LostKeyboardFocus="ProjectOptionsList_LostKeyboardFocus" ItemContainerStyle="{StaticResource ProjectOptionListBoxItem}">
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto" />
                                                        <RowDefinition Height="Auto" />
                                                    </Grid.RowDefinitions>

                                                    <TextBlock Text="{Binding ProjectName}" Foreground="{DynamicResource ThemeBrush_AccentText_0F172A}" FontSize="13" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />

                                                    <TextBlock Grid.Row="1" Margin="0,3,0,0" Foreground="{DynamicResource ThemeBrush_TextSecondary_64748B}" FontSize="11" TextTrimming="CharacterEllipsis">
                                                        <TextBlock.Text>
                                                            <MultiBinding StringFormat="{}{0} • {1}">
                                                                <Binding Path="Company" />
                                                                <Binding Path="SourceOfFund" />
                                                            </MultiBinding>
                                                        </TextBlock.Text>
                                                    </TextBlock>
                                                </Grid>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>
                                </Border>
                            </Popup>
                        </Grid>
                    </StackPanel>

                    <StackPanel Grid.Column="2" VerticalAlignment="Center">
                        <TextBlock Text="{Binding SelectedProject.Company, TargetNullValue='No project selected'}" Foreground="{DynamicResource ThemeBrush_AccentText_0F172A}" FontSize="15" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />

                        <TextBlock Text="{Binding SelectedProjectSummary}" Foreground="{DynamicResource ThemeBrush_TextSecondary_64748B}" FontSize="12" Margin="0,6,0,0" TextWrapping="Wrap" />
                    </StackPanel>

                    <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center">
                        <StackPanel Margin="0,0,18,0" VerticalAlignment="Center">
                            <TextBlock Text="TOTAL BUDGET" Foreground="{DynamicResource ThemeBrush_TextSecondary_64748B}" FontSize="10" FontWeight="Bold" HorizontalAlignment="Right" />
                            <TextBlock Text="{Binding TotalBudgetText}" Foreground="{DynamicResource ThemeBrush_AccentText_0F172A}" FontSize="20" FontWeight="Bold" Margin="0,4,0,0" HorizontalAlignment="Right" />
                        </StackPanel>

                        <Button MinWidth="158" Height="46" Margin="0,0,10,0" Style="{StaticResource LightButton}" Command="{Binding GenerateReleaseReportCommand}" Content="{Binding GenerateReportButtonText}"  IsEnabled="{Binding HasSelectedProject}"/>

                        <Button MinWidth="184" Height="46" Style="{StaticResource PrimaryButton}" Command="{Binding OpenReleaseSessionCommand}" Content="Start Release Session"  IsEnabled="{Binding HasSelectedProject}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- OPERATIONAL SUMMARY -->
            <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,14">
                <Border Style="{StaticResource MetricCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="46" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Width="38" Height="38" CornerRadius="12" Background="{DynamicResource ThemeBrush_InfoSoft_EFF6FF}">
                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="" Foreground="{DynamicResource ThemeBrush_Info_2563EB}" FontSize="17" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="TOTAL BENEFICIARIES" Foreground="{DynamicResource ThemeBrush_TextSecondary_64748B}" FontSize="10" FontWeight="Bold" />
                            <TextBlock Text="{Binding TotalBeneficiariesCount}" Foreground="{DynamicResource ThemeBrush_AccentText_0F172A}" FontSize="23" FontWeight="Bold" Margin="0,3,0,0" />
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Style="{StaticResource MetricCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="46" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Width="38" Height="38" CornerRadius="12" Background="{DynamicResource ThemeBrush_SuccessSoft_ECFDF3}">
                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="" Foreground="{DynamicResource ThemeBrush_Success_16A34A}" FontSize="17" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="RELEASED" Foreground="{DynamicResource ThemeBrush_TextSecondary_64748B}" FontSize="10" FontWeight="Bold" />
                            <TextBlock Text="{Binding ReleasedCount, Mode=OneWay}" Foreground="{DynamicResource ThemeBrush_Success_16A34A}" FontSize="23" FontWeight="Bold" Margin="0,3,0,0" />
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Style="{StaticResource MetricCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="46" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Width="38" Height="38" CornerRadius="12" Background="{DynamicResource ThemeBrush_WarningSoft_FFF7E8}">
                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="" Foreground="{DynamicResource ThemeBrush_Warning_D97706}" FontSize="17" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="WAITING" Foreground="{DynamicResource ThemeBrush_TextSecondary_64748B}" FontSize="10" FontWeight="Bold" />
                            <TextBlock Text="{Binding RemainingCount, Mode=OneWay}" Foreground="{DynamicResource ThemeBrush_Warning_D97706}" FontSize="23" FontWeight="Bold" Margin="0,3,0,0" />
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Style="{StaticResource MetricCardStyle}" Margin="0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="46" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Width="38" Height="38" CornerRadius="12" Background="{DynamicResource ThemeBrush_Accent_EEF2FF}">
                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="" Foreground="{DynamicResource ThemeBrush_AccentText_1F2A44}" FontSize="17" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="COMPLETION" Foreground="{DynamicResource ThemeBrush_TextSecondary_64748B}" FontSize="10" FontWeight="Bold" />
                            <TextBlock Text="{Binding CompletionText}" Foreground="{DynamicResource ThemeBrush_AccentText_1F2A44}" FontSize="19" FontWeight="Bold" Margin="0,3,0,0" />
                        </StackPanel>
                    </Grid>
                </Border>
            </UniformGrid>

            <!-- QUEUE + RELEASE STATION -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2.25*" />
                    <ColumnDefinition Width="18" />
                    <ColumnDefinition Width="0.85*" />
                </Grid.ColumnDefinitions>

                <!-- DISTRIBUTION QUEUE -->
                <Border Grid.Column="0" CornerRadius="22" Background="{DynamicResource ThemeBrush_Surface_FFFFFF}" BorderBrush="{DynamicResource ThemeBrush_Border_F1F5F9}" BorderThickness="1" Padding="18" Effect="{StaticResource CardShadow}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel>
                                <TextBlock Text="Distribution Queue" Foreground="{DynamicResource ThemeBrush_AccentText_0F172A}" FontSize="18" FontWeight="Bold" />
                                <TextBlock Text="{Binding FoundText}" Foreground="{DynamicResource ThemeBrush_TextSecondary_64748B}" FontSize="11" Margin="0,4,0,0" />
                            </StackPanel>

                            <Border Grid.Column="1" Background="{DynamicResource ThemeBrush_InfoSoft_F8FAFC}" BorderBrush="{DynamicResource ThemeBrush_Border_E7ECF5}" BorderThickness="1" CornerRadius="12" Padding="4" Margin="0,0,10,0">
                                <ItemsControl ItemsSource="{Binding DistributionStatusOptions}" IsEnabled="{Binding HasSelectedProject}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Button Content="{Binding}" Command="{Binding DataContext.SetDistributionStatusCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                                                <Button.Style>
                                                    <Style TargetType="Button" BasedOn="{StaticResource SegmentButtonStyle}">
                                                        <Style.Triggers>
                                                            <DataTrigger Value="True">
                                                                <DataTrigger.Binding>
                                                                    <MultiBinding Converter="{StaticResource EqualsToBoolConverter}">
                                                                        <Binding />
                                                                        <Binding Path="DataContext.SelectedDistributionStatus" RelativeSource="{RelativeSource AncestorType=UserControl}" />
                                                                    </MultiBinding>
                                                                </DataTrigger.Binding>
                                                                <Setter Property="Background" Value="{DynamicResource ThemeBrush_Accent_1F2A44}" />
                                                                <Setter Property="Foreground" Value="{DynamicResource ThemeBrush_TextPrimary_FFFFFF}" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Border>

                            <Border Grid.Column="2" Width="150" CornerRadius="12" BorderBrush="{DynamicResource ThemeBrush_Border_E7ECF5}" BorderThickness="1" Background="{DynamicResource ThemeBrush_InfoSoft_FCFDFE}">
                                <ComboBox ItemsSource="{Binding ClassificationOptions}" SelectedItem="{Binding SelectedClassification, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernComboBox}" ToolTip="Filter by classification"  IsEnabled="{Binding HasSelectedProject}"/>
                            </Border>
                        </Grid>

                        <Border Grid.Row="1" Height="1" Background="{DynamicResource ThemeBrush_Border_F1F5F9}" Margin="0,14,0,6" />

                                                <Grid Grid.Row="2">
                            <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="Column" GridLinesVisibility="None" RowHeaderWidth="0" Background="Transparent" BorderThickness="0" FontSize="12" Foreground="{DynamicResource ThemeBrush_AccentText_0F172A}" ColumnHeaderHeight="48" RowHeight="56" IsReadOnly="True" SelectionUnit="FullRow" SelectionMode="Single" FocusVisualStyle="{x:Null}" ColumnHeaderStyle="{StaticResource QueueColumnHeaderStyle}" RowStyle="{StaticResource QueueRowStyle}" CellStyle="{StaticResource QueueCellStyle}" CanUserReorderColumns="False" CanUserResizeRows="False">
                                                        <DataGrid.Columns><DataGridTemplateColumn Header="Beneficiary" Width="2.0*" HeaderStyle="{StaticResource QueueLeftColumnHeaderStyle}"><DataGridTemplateColumn.CellTemplate><DataTemplate><Grid VerticalAlignment="Center"><Grid.ColumnDefinitions><ColumnDefinition Width="40" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions><Border Width="32" Height="32" CornerRadius="16" Background="{DynamicResource ThemeBrush_Accent_EEF2FF}" VerticalAlignment="Center"><TextBlock FontFamily="Segoe MDL2 Assets" Text="" Foreground="{DynamicResource ThemeBrush_AccentText_1F2A44}" FontSize="15" HorizontalAlignment="Center" VerticalAlignment="Center" /></Border><StackPanel Grid.Column="1" VerticalAlignment="Center"><TextBlock Foreground="{DynamicResource ThemeBrush_AccentText_0F172A}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"><TextBlock.Text><MultiBinding StringFormat="{}{0} {1}"><Binding Path="FirstName" /><Binding Path="LastName" /></MultiBinding></TextBlock.Text></TextBlock><TextBlock Text="{Binding BeneficiaryId, TargetNullValue='No beneficiary ID'}" Foreground="{DynamicResource ThemeBrush_TextSecondary_64748B}" FontSize="10" Margin="0,2,0,0" TextTrimming="CharacterEllipsis" /></StackPanel></Grid></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn><DataGridTextColumn Header="Gender" Binding="{Binding Gender}" Width="0.70*"><DataGridTextColumn.ElementStyle><Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Stretch" /><Setter Property="VerticalAlignment" Value="Center" /><Setter Property="TextAlignment" Value="Center" /></Style></DataGridTextColumn.ElementStyle></DataGridTextColumn><DataGridTextColumn Header="Barangay" Binding="{Binding Barangay}" Width="1.0*"><DataGridTextColumn.ElementStyle><Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Stretch" /><Setter Property="VerticalAlignment" Value="Center" /><Setter Property="TextAlignment" Value="Center" /><Setter Property="TextTrimming" Value="CharacterEllipsis" /></Style></DataGridTextColumn.ElementStyle></DataGridTextColumn><DataGridTemplateColumn Header="Classification" Width="1.0*"><DataGridTemplateColumn.CellTemplate><DataTemplate><Border Height="26" MinWidth="58" MaxWidth="150" Padding="10,0" HorizontalAlignment="Center" VerticalAlignment="Center" BorderThickness="1" CornerRadius="13" SnapsToDevicePixels="True"><Border.Style><Style TargetType="Border"><Setter Property="Background" Value="{DynamicResource ThemeBrush_InfoSoft_F1F5F9}" /><Setter Property="BorderBrush" Value="{DynamicResource ThemeBrush_InfoBorder_E2E8F0}" /><Style.Triggers><DataTrigger Binding="{Binding Classification}" Value="PWD"><Setter Property="Background" Value="{DynamicResource ThemeBrush_PurpleSoft_F3E8FF}" /><Setter Property="BorderBrush" Value="{DynamicResource ThemeBrush_PurpleBorder_E9D5FF}" /></DataTrigger><DataTrigger Binding="{Binding Classification}" Value="Farmer"><Setter Property="Background" Value="{DynamicResource ThemeBrush_SuccessSoft_ECFDF3}" /><Setter Property="BorderBrush" Value="{DynamicResource ThemeBrush_SuccessBorder_BBF7D0}" /></DataTrigger><DataTrigger Binding="{Binding Classification}" Value="Vendor"><Setter Property="Background" Value="{DynamicResource ThemeBrush_InfoSoft_EFF6FF}" /><Setter Property="BorderBrush" Value="{DynamicResource ThemeBrush_InfoBorder_BFDBFE}" /></DataTrigger><DataTrigger Binding="{Binding Classification}" Value="Senior Citizen"><Setter Property="Background" Value="{DynamicResource ThemeBrush_WarningSoft_FFF7ED}" /><Setter Property="BorderBrush" Value="{DynamicResource ThemeBrush_WarningBorder_FED7AA}" /></DataTrigger><DataTrigger Binding="{Binding Classification}" Value="Indigenous"><Setter Property="Background" Value="{DynamicResource ThemeBrush_TealSoft_F0FDFA}" /><Setter Property="BorderBrush" Value="{DynamicResource ThemeBrush_TealBorder_99F6E4}" /></DataTrigger><DataTrigger Binding="{Binding Classification}" Value="Indigenous People"><Setter Property="Background" Value="{DynamicResource ThemeBrush_TealSoft_F0FDFA}" /><Setter Property="BorderBrush" Value="{DynamicResource ThemeBrush_TealBorder_99F6E4}" /></DataTrigger></Style.Triggers></Style></Border.Style><StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center"><Ellipse Width="6" Height="6" Margin="0,0,7,0" VerticalAlignment="Center"><Ellipse.Style><Style TargetType="Ellipse"><Setter Property="Fill" Value="{DynamicResource ThemeBrush_SurfaceMuted_94A3B8}" /><Style.Triggers><DataTrigger Binding="{Binding Classification}" Value="PWD"><Setter Property="Fill" Value="{DynamicResource ThemeBrush_Purple_7E22CE}" /></DataTrigger><DataTrigger Binding="{Binding Classification}" Value="Farmer"><Setter Property="Fill" Value="{DynamicResource ThemeBrush_Success_16A34A}" /></DataTrigger><DataTrigger Binding="{Binding Classification}" Value="Vendor"><Setter Property="Fill" Value="{DynamicResource ThemeBrush_Info_2563EB}" /></DataTrigger><DataTrigger Binding="{Binding Classification}" Value="Senior Citizen"><Setter Property="Fill" Value="{DynamicResource ThemeBrush_Danger_C2410C}" /></DataTrigger><DataTrigger Binding="{Binding Classification}" Value="Indigenous"><Setter Property="Fill" Value="{DynamicResource ThemeBrush_Accent_0F766E}" /></DataTrigger><DataTrigger Binding="{Binding Classification}" Value="Indigenous People"><Setter Property="Fill" Value="{DynamicResource ThemeBrush_Accent_0F766E}" /></DataTrigger></Style.Triggers></Style></Ellipse.Style></Ellipse><TextBlock Text="{Binding Classification, TargetNullValue=None, FallbackValue=None}" FontSize="11" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"><TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Foreground" Value="{DynamicResource ThemeBrush_TextSecondary_64748B}" /><Style.Triggers><DataTrigger Binding="{Binding Classification}" Value="PWD"><Setter Property="Foreground" Value="{DynamicResource ThemeBrush_Purple_7E22CE}" /></DataTrigger><DataTrigger Binding="{Binding Classification}" Value="Farmer"><Setter Property="Foreground" Value="{DynamicResource ThemeBrush_Success_15803D}" /></DataTrigger><DataTrigger Binding="{Binding Classification}" Value="Vendor"><Setter Property="Foreground" Value="{DynamicResource ThemeBrush_Info_1D4ED8}" /></DataTrigger><DataTrigger Binding="{Binding Classification}" Value="Senior Citizen"><Setter Property="Foreground" Value="{DynamicResource ThemeBrush_Danger_C2410C}" /></DataTrigger><DataTrigger Binding="{Binding Classification}" Value="Indigenous"><Setter Property="Foreground" Value="{DynamicResource ThemeBrush_Teal_0F766E}" /></DataTrigger><DataTrigger Binding="{Binding Classification}" Value="Indigenous People"><Setter Property="Foreground" Value="{DynamicResource ThemeBrush_Teal_0F766E}" /></DataTrigger></Style.Triggers></Style></TextBlock.Style></TextBlock></StackPanel></Border></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn><DataGridTextColumn Header="Allocated Share" Binding="{Binding ShareText}" Width="1.0*"><DataGridTextColumn.ElementStyle><Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Stretch" /><Setter Property="VerticalAlignment" Value="Center" /><Setter Property="TextAlignment" Value="Center" /><Setter Property="FontWeight" Value="SemiBold" /></Style></DataGridTextColumn.ElementStyle></DataGridTextColumn><DataGridTemplateColumn Header="Release Status" Width="0.95*"><DataGridTemplateColumn.CellTemplate><DataTemplate><Border Height="26" MinWidth="82" Padding="10,0" HorizontalAlignment="Center" VerticalAlignment="Center" BorderThickness="1" CornerRadius="13" SnapsToDevicePixels="True"><Border.Style><Style TargetType="Border"><Setter Property="Background" Value="{DynamicResource ThemeBrush_WarningSoft_FFF7E8}" /><Setter Property="BorderBrush" Value="{DynamicResource ThemeBrush_WarningBorder_FDE7B2}" /><Style.Triggers><DataTrigger Binding="{Binding IsReleased}" Value="True"><Setter Property="Background" Value="{DynamicResource ThemeBrush_SuccessSoft_ECFDF3}" /><Setter Property="BorderBrush" Value="{DynamicResource ThemeBrush_SuccessBorder_BBF7D0}" /></DataTrigger></Style.Triggers></Style></Border.Style><StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center"><Ellipse Width="6" Height="6" Margin="0,0,7,0" VerticalAlignment="Center"><Ellipse.Style><Style TargetType="Ellipse"><Setter Property="Fill" Value="{DynamicResource ThemeBrush_Warning_D97706}" /><Style.Triggers><DataTrigger Binding="{Binding IsReleased}" Value="True"><Setter Property="Fill" Value="{DynamicResource ThemeBrush_Success_16A34A}" /></DataTrigger></Style.Triggers></Style></Ellipse.Style></Ellipse><TextBlock FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center"><TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Text" Value="Waiting" /><Setter Property="Foreground" Value="{DynamicResource ThemeBrush_Warning_B45309}" /><Style.Triggers><DataTrigger Binding="{Binding IsReleased}" Value="True"><Setter Property="Text" Value="Released" /><Setter Property="Foreground" Value="{DynamicResource ThemeBrush_Success_15803D}" /></DataTrigger></Style.Triggers></Style></TextBlock.Style></TextBlock></StackPanel></Border></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn></DataGrid.Columns>
                                                    </DataGrid>
                        <!-- EKALINGA_EXACT_LIGHT_DARK_V1_PROJECT_EMPTY_DISTRIBUTION -->
                        <Border Panel.ZIndex="20"
                                Background="{DynamicResource ThemeBrush_Surface_FFFFFF}"
                                BorderBrush="{DynamicResource ThemeBrush_Border_F1F5F9}"
                                BorderThickness="1"
                                CornerRadius="16"
                                Margin="0,8,0,0"
                                Padding="28">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasSelectedProject}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="420">
                                <Border Width="56" Height="56" CornerRadius="18" Background="{DynamicResource ThemeBrush_Accent_EEF2FF}" HorizontalAlignment="Center">
                                    <TextBlock Text="&#xE8A5;" FontFamily="Segoe MDL2 Assets" FontSize="23" Foreground="{DynamicResource ThemeBrush_AccentText_1F2A44}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="Select an active project to view its details." Foreground="{DynamicResource ThemeBrush_AccentText_0F172A}" FontSize="16" FontWeight="SemiBold" TextAlignment="Center" TextWrapping="Wrap" Margin="0,14,0,0"/>
                            </StackPanel>
                        </Border>
                        </Grid>

                        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,14,0,0">
                            <Button Content="Previous" Style="{StaticResource PageButton}" Command="{Binding PreviousPageCommand}" />

                            <ItemsControl ItemsSource="{Binding PageNumbers}" Margin="8,0">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Button Content="{Binding}" Command="{Binding DataContext.GoToPageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource PageButton}">
                                                    <Style.Triggers>
                                                        <DataTrigger Value="True">
                                                            <DataTrigger.Binding>
                                                                <MultiBinding Converter="{StaticResource EqualsToBoolConverter}">
                                                                    <Binding />
                                                                    <Binding Path="DataContext.CurrentPage" RelativeSource="{RelativeSource AncestorType=UserControl}" />
                                                                </MultiBinding>
                                                            </DataTrigger.Binding>
                                                            <Setter Property="Background" Value="{DynamicResource ThemeBrush_Accent_1F2A44}" />
                                                            <Setter Property="BorderBrush" Value="{DynamicResource ThemeBrush_Accent_1F2A44}" />
                                                            <Setter Property="Foreground" Value="{DynamicResource ThemeBrush_TextPrimary_FFFFFF}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <Button Content="Next" Style="{StaticResource PageButton}" Command="{Binding NextPageCommand}" />
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- RELEASE STATION -->
                <Border Grid.Column="2" CornerRadius="22" Background="{DynamicResource ThemeBrush_Accent_1F2A44}" Padding="22" Effect="{StaticResource CardShadow}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Border Width="58" Height="58" CornerRadius="18" HorizontalAlignment="Left" Background="{DynamicResource ThemeBrush_Glass_22FFFFFF}">
                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="" Foreground="{DynamicResource ThemeBrush_TextPrimary_FFFFFF}" FontSize="25" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>

                        <StackPanel Grid.Row="1" Margin="0,18,0,0">
                            <TextBlock Text="Release Station" Foreground="{DynamicResource ThemeBrush_OnAccent_FFFFFF}" FontSize="21" FontWeight="Bold" />
                            <TextBlock Text="Ready for beneficiary barcode or ID scanning." Foreground="{DynamicResource ThemeBrush_Info_C9D3E7}" FontSize="12" Margin="0,6,0,0" TextWrapping="Wrap" />
                        </StackPanel>

                        <Border Grid.Row="2" Margin="0,20,0,0" Padding="14" CornerRadius="14" Background="{DynamicResource ThemeBrush_Glass_15FFFFFF}" BorderBrush="{DynamicResource ThemeBrush_Glass_24FFFFFF}" BorderThickness="1">
                            <StackPanel>
                                <Grid>
                                    <TextBlock Text="Distribution progress" Foreground="{DynamicResource ThemeBrush_Info_C9D3E7}" FontSize="11" />
                                    <TextBlock Text="{Binding CompletionText}" Foreground="{DynamicResource ThemeBrush_TextPrimary_FFFFFF}" FontSize="12" FontWeight="SemiBold" HorizontalAlignment="Right" />
                                </Grid>

                                <ProgressBar Height="8" Margin="0,10,0,0" Maximum="100" Value="{Binding CompletionPercentage, Mode=OneWay}" Foreground="{DynamicResource ThemeBrush_Success_5EE28A}" Background="{DynamicResource ThemeBrush_Accent_2E3A59}" />

                                <Grid Margin="0,12,0,0">
                                    <TextBlock Foreground="{DynamicResource ThemeBrush_Info_C9D3E7}" FontSize="11">
                                        <Run Text="Waiting: " />
                                        <Run Text="{Binding RemainingCount, Mode=OneWay}" Foreground="{DynamicResource ThemeBrush_TextPrimary_FFFFFF}" FontWeight="Bold" />
                                    </TextBlock>
                                    <TextBlock HorizontalAlignment="Right" Foreground="{DynamicResource ThemeBrush_Info_C9D3E7}" FontSize="11">
                                        <Run Text="Released: " />
                                        <Run Text="{Binding ReleasedCount, Mode=OneWay}" Foreground="{DynamicResource ThemeBrush_TextPrimary_FFFFFF}" FontWeight="Bold" />
                                    </TextBlock>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <StackPanel Grid.Row="3" Margin="0,20,0,0">
                            <TextBlock Text="How it works" Foreground="{DynamicResource ThemeBrush_TextPrimary_FFFFFF}" FontWeight="SemiBold" FontSize="13" />

                            <TextBlock Text="1  Open the release session." Foreground="{DynamicResource ThemeBrush_Info_C9D3E7}" FontSize="12" Margin="0,12,0,0" />
                            <TextBlock Text="2  Scan the beneficiary ID." Foreground="{DynamicResource ThemeBrush_Info_C9D3E7}" FontSize="12" Margin="0,8,0,0" />
                            <TextBlock Text="3  Verify details and confirm." Foreground="{DynamicResource ThemeBrush_Info_C9D3E7}" FontSize="12" Margin="0,8,0,0" />
                        </StackPanel>

                        <StackPanel Grid.Row="4" Margin="0,22,0,0">
                            <Button Height="48" Background="{DynamicResource ThemeBrush_Surface_FFFFFF}" Foreground="{DynamicResource ThemeBrush_AccentText_1F2A44}" BorderBrush="Transparent" FontWeight="Bold" Cursor="Hand" Command="{Binding OpenReleaseSessionCommand}" Content="Open Release Station" IsEnabled="{Binding HasSelectedProject}">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="13">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeBrush_InfoSoft_EEF2FF}" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>

                            <Button Height="42" Margin="0,10,0,0" Background="{DynamicResource ThemeBrush_Glass_16FFFFFF}" Foreground="{DynamicResource ThemeBrush_TextPrimary_FFFFFF}" BorderBrush="{DynamicResource ThemeBrush_Glass_2AFFFFFF}" BorderThickness="1" FontWeight="SemiBold" Cursor="Hand" Command="{Binding GenerateReleaseReportCommand}" Content="{Binding GenerateReportButtonText}" IsEnabled="{Binding HasSelectedProject}">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="13">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeBrush_Glass_24FFFFFF}" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
<!-- TOAST -->
        <Border Panel.ZIndex="1000" Background="{Binding ToastBackground}" CornerRadius="14" Padding="14,10" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,20,20,0" Visibility="{Binding IsToastVisible, Converter={StaticResource BoolToVis}}">
            <Border.Effect>
                <DropShadowEffect Color="{DynamicResource ThemeColor_Shadow_1E293B}" BlurRadius="18" ShadowDepth="0" Opacity="0.16" />
            </Border.Effect>
            <TextBlock Text="{Binding ToastMessage}" Foreground="{DynamicResource ThemeBrush_TextPrimary_FFFFFF}" FontWeight="SemiBold" />
        </Border>

        <!-- RELEASE SESSION MODAL -->
        <Grid Panel.ZIndex="900" Background="{DynamicResource ThemeBrush_Page_F6F8FC}" Visibility="{Binding IsReleaseSessionOpen, Converter={StaticResource BoolToVis}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="76" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- WORKSTATION HEADER -->
            <Border Grid.Row="0" Background="{DynamicResource ThemeBrush_Accent_1F2A44}" Padding="24,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="18" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="18" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Border Width="42" Height="42" CornerRadius="13" Background="{DynamicResource ThemeBrush_Glass_20FFFFFF}" VerticalAlignment="Center">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="" Foreground="{DynamicResource ThemeBrush_TextPrimary_FFFFFF}" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>

                    <StackPanel Grid.Column="2" VerticalAlignment="Center">
                        <TextBlock Text="Release Station" Foreground="{DynamicResource ThemeBrush_OnAccent_FFFFFF}" FontSize="19" FontWeight="Bold" />
                        <TextBlock Text="{Binding SelectedProject.ProjectName}" Foreground="{DynamicResource ThemeBrush_Info_C9D3E7}" FontSize="12" Margin="0,4,0,0" />
                    </StackPanel>

                    <StackPanel Grid.Column="3" VerticalAlignment="Center">
                        <TextBlock Text="{Binding ReleaseCompletionText}" Foreground="{DynamicResource ThemeBrush_TextPrimary_FFFFFF}" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Right" />
                        <TextBlock Text="{Binding TotalBudgetText}" Foreground="{DynamicResource ThemeBrush_Info_C9D3E7}" FontSize="11" Margin="0,4,0,0" HorizontalAlignment="Right" />
                    </StackPanel>

                    <Button Grid.Column="5" Width="38" Height="38" Background="{DynamicResource ThemeBrush_Glass_18FFFFFF}" BorderBrush="Transparent" Cursor="Hand" Command="{Binding CloseReleaseSessionCommand}">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="12">
                                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="" Foreground="{DynamicResource ThemeBrush_TextPrimary_FFFFFF}" FontSize="13" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeBrush_Glass_2AFFFFFF}" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="350" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- SCANNER CONSOLE -->
                <Border Grid.Column="0" CornerRadius="22" Background="{DynamicResource ThemeBrush_Accent_1F2A44}" Padding="22" Effect="{StaticResource CardShadow}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Border Width="46" Height="46" CornerRadius="15" Background="{DynamicResource ThemeBrush_Glass_20FFFFFF}">
                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="" Foreground="{DynamicResource ThemeBrush_TextPrimary_FFFFFF}" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>

                            <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="SCANNER READY" Foreground="{DynamicResource ThemeBrush_Success_67E89A}" FontSize="11" FontWeight="Bold" />
                                <TextBlock Text="Waiting for beneficiary ID" Foreground="{DynamicResource ThemeBrush_TextPrimary_FFFFFF}" FontSize="14" FontWeight="SemiBold" Margin="0,4,0,0" />
                            </StackPanel>
                        </Grid>

                        <StackPanel Grid.Row="1" Margin="0,24,0,0">
                            <TextBlock Text="SCANNED BENEFICIARY ID" Foreground="{DynamicResource ThemeBrush_Info_C9D3E7}" FontSize="10" FontWeight="Bold" Margin="0,0,0,8" />

                            <Border Height="54" CornerRadius="14" Background="{DynamicResource ThemeBrush_Surface_FFFFFF}" BorderBrush="{DynamicResource ThemeBrush_Glass_22FFFFFF}" BorderThickness="1" Padding="16,0">
                                <Grid>
                                    <TextBox IsReadOnly="True" IsTabStop="False" Cursor="Arrow" Text="{Binding ScanInput, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" Foreground="{DynamicResource ThemeBrush_AccentText_1F2A44}" FontSize="16" FontWeight="Bold" VerticalContentAlignment="Center" />

                                    <TextBlock Text="Scan now..." Foreground="{DynamicResource ThemeBrush_TextSecondary_94A3B8}" FontSize="14" VerticalAlignment="Center" IsHitTestVisible="False">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ScanInput}" Value="">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ScanInput}" Value="{x:Null}">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>
                            </Border>
                        </StackPanel>

                        <Border Grid.Row="2" Margin="0,20,0,0" Padding="15" CornerRadius="14" Background="{DynamicResource ThemeBrush_Glass_13FFFFFF}" BorderBrush="{DynamicResource ThemeBrush_Glass_20FFFFFF}" BorderThickness="1">
                            <StackPanel>
                                <Grid>
                                    <TextBlock Text="Session progress" Foreground="{DynamicResource ThemeBrush_Info_C9D3E7}" FontSize="11" />
                                    <TextBlock Text="{Binding ReleaseCompletionPercentage, StringFormat={}{0:0.#}%}" Foreground="{DynamicResource ThemeBrush_TextPrimary_FFFFFF}" FontWeight="Bold" HorizontalAlignment="Right" />
                                </Grid>

                                <ProgressBar Height="8" Margin="0,10,0,0" Maximum="100" Value="{Binding ReleaseCompletionPercentage, Mode=OneWay}" Foreground="{DynamicResource ThemeBrush_Success_5EE28A}" Background="{DynamicResource ThemeBrush_Accent_2E3A59}" />

                                <Grid Margin="0,12,0,0">
                                    <TextBlock Foreground="{DynamicResource ThemeBrush_Info_C9D3E7}" FontSize="11">
                                        <Run Text="Remaining " />
                                        <Run Text="{Binding ReleaseRemainingCount, Mode=OneWay}" Foreground="{DynamicResource ThemeBrush_TextPrimary_FFFFFF}" FontWeight="Bold" />
                                    </TextBlock>
                                    <TextBlock HorizontalAlignment="Right" Foreground="{DynamicResource ThemeBrush_Info_C9D3E7}" FontSize="11">
                                        <Run Text="Released " />
                                        <Run Text="{Binding ReleaseReleasedCount, Mode=OneWay}" Foreground="{DynamicResource ThemeBrush_TextPrimary_FFFFFF}" FontWeight="Bold" />
                                    </TextBlock>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <StackPanel Grid.Row="3" Margin="0,20,0,0">
                            <TextBlock Text="CLASSIFICATION" Foreground="{DynamicResource ThemeBrush_Info_C9D3E7}" FontSize="10" FontWeight="Bold" Margin="0,0,0,8" />
                            <Border CornerRadius="13" Background="{DynamicResource ThemeBrush_Surface_FFFFFF}" BorderBrush="{DynamicResource ThemeBrush_Glass_20FFFFFF}" BorderThickness="1">
                                <ComboBox ItemsSource="{Binding ClassificationOptions}" SelectedItem="{Binding ReleaseSelectedClassification}" Style="{StaticResource ModernComboBox}" />
                            </Border>
                        </StackPanel>

                        <StackPanel Grid.Row="4" Margin="0,22,0,0">
                            <TextBlock Text="Scanner instructions" Foreground="{DynamicResource ThemeBrush_TextPrimary_FFFFFF}" FontSize="13" FontWeight="SemiBold" />
                            <TextBlock Text="Keep this window active, then scan the beneficiary barcode or type through the configured scanner. Verify the profile before confirming release." Foreground="{DynamicResource ThemeBrush_Info_C9D3E7}" FontSize="12" LineHeight="19" TextWrapping="Wrap" Margin="0,10,0,0" />
                        </StackPanel>

                        <Button Grid.Row="5" Height="44" Margin="0,20,0,0" Background="{DynamicResource ThemeBrush_Surface_FFFFFF}" Foreground="{DynamicResource ThemeBrush_AccentText_1F2A44}" BorderBrush="Transparent" FontWeight="SemiBold" Cursor="Hand" Command="{Binding GenerateReleaseReportCommand}" Content="{Binding GenerateReportButtonText}" IsEnabled="{Binding HasSelectedProject}">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="12">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeBrush_InfoSoft_EEF2FF}" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>
                </Border>

                <!-- SESSION QUEUE -->
                <Border Grid.Column="2" CornerRadius="22" Background="{DynamicResource ThemeBrush_Surface_FFFFFF}" BorderBrush="{DynamicResource ThemeBrush_Border_F1F5F9}" BorderThickness="1" Padding="18" Effect="{StaticResource CardShadow}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Grid>
                            <StackPanel>
                                <TextBlock Text="Release Queue" Foreground="{DynamicResource ThemeBrush_AccentText_0F172A}" FontSize="18" FontWeight="Bold" />
                                <TextBlock Text="Released beneficiaries remain visible and are dimmed." Foreground="{DynamicResource ThemeBrush_TextSecondary_64748B}" FontSize="11" Margin="0,4,0,0" />
                            </StackPanel>

                            <Border HorizontalAlignment="Right" VerticalAlignment="Center" Background="{DynamicResource ThemeBrush_Accent_EEF2FF}" CornerRadius="12" Padding="12,7">
                                <TextBlock Text="{Binding ReleaseProgressText}" Foreground="{DynamicResource ThemeBrush_AccentText_1F2A44}" FontSize="11" FontWeight="SemiBold" />
                            </Border>
                        </Grid>

                        <Border Grid.Row="1" Height="1" Background="{DynamicResource ThemeBrush_Border_F1F5F9}" Margin="0,14,0,6" />

                        <DataGrid Grid.Row="2" ItemsSource="{Binding ReleasePagedItems}" SelectedItem="{Binding SelectedReleaseRow}" AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="Column" GridLinesVisibility="None" RowHeaderWidth="0" Background="Transparent" BorderThickness="0" FontSize="12" Foreground="{DynamicResource ThemeBrush_AccentText_0F172A}" ColumnHeaderHeight="48" RowHeight="56" IsReadOnly="True" SelectionUnit="FullRow" SelectionMode="Single" FocusVisualStyle="{x:Null}" ColumnHeaderStyle="{StaticResource QueueColumnHeaderStyle}" CellStyle="{StaticResource QueueCellStyle}" CanUserReorderColumns="False" CanUserResizeRows="False">
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow" BasedOn="{StaticResource QueueRowStyle}">
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource ThemeBrush_InfoSoft_FBFCFF}" />
                                        <Setter Property="BorderBrush" Value="{DynamicResource ThemeBrush_InfoBorder_D8E1F0}" /></Trigger>
                                        <DataTrigger Binding="{Binding IsReleased}" Value="True">
                                            <Setter Property="Opacity" Value="0.50" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>

                            <DataGrid.Columns><DataGridTemplateColumn Header="Beneficiary" Width="2.0*" HeaderStyle="{StaticResource QueueLeftColumnHeaderStyle}"><DataGridTemplateColumn.CellTemplate><DataTemplate><Grid VerticalAlignment="Center"><Grid.ColumnDefinitions><ColumnDefinition Width="40" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions><Border Width="32" Height="32" CornerRadius="16" Background="{DynamicResource ThemeBrush_Accent_EEF2FF}" VerticalAlignment="Center"><TextBlock FontFamily="Segoe MDL2 Assets" Text="" Foreground="{DynamicResource ThemeBrush_AccentText_1F2A44}" FontSize="15" HorizontalAlignment="Center" VerticalAlignment="Center" /></Border><StackPanel Grid.Column="1" VerticalAlignment="Center"><TextBlock Foreground="{DynamicResource ThemeBrush_AccentText_0F172A}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"><TextBlock.Text><MultiBinding StringFormat="{}{0} {1}"><Binding Path="FirstName" /><Binding Path="LastName" /></MultiBinding></TextBlock.Text></TextBlock><TextBlock Text="{Binding BeneficiaryId, TargetNullValue='No beneficiary ID'}" Foreground="{DynamicResource ThemeBrush_TextSecondary_64748B}" FontSize="10" Margin="0,2,0,0" TextTrimming="CharacterEllipsis" /></StackPanel></Grid></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn><DataGridTextColumn Header="Gender" Binding="{Binding Gender}" Width="0.70*"><DataGridTextColumn.ElementStyle><Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Stretch" /><Setter Property="VerticalAlignment" Value="Center" /><Setter Property="TextAlignment" Value="Center" /></Style></DataGridTextColumn.ElementStyle></DataGridTextColumn><DataGridTextColumn Header="Barangay" Binding="{Binding Barangay}" Width="1.0*"><DataGridTextColumn.ElementStyle><Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Stretch" /><Setter Property="VerticalAlignment" Value="Center" /><Setter Property="TextAlignment" Value="Center" /><Setter Property="TextTrimming" Value="CharacterEllipsis" /></Style></DataGridTextColumn.ElementStyle></DataGridTextColumn><DataGridTemplateColumn Header="Classification" Width="1.0*"><DataGridTemplateColumn.CellTemplate><DataTemplate><Border Height="26" MinWidth="58" MaxWidth="150" Padding="10,0" HorizontalAlignment="Center" VerticalAlignment="Center" BorderThickness="1" CornerRadius="13" SnapsToDevicePixels="True"><Border.Style><Style TargetType="Border"><Setter Property="Background" Value="{DynamicResource ThemeBrush_InfoSoft_F1F5F9}" /><Setter Property="BorderBrush" Value="{DynamicResource ThemeBrush_InfoBorder_E2E8F0}" /><Style.Triggers><DataTrigger Binding="{Binding Classification}" Value="PWD"><Setter Property="Background" Value="{DynamicResource ThemeBrush_PurpleSoft_F3E8FF}" /><Setter Property="BorderBrush" Value="{DynamicResource ThemeBrush_PurpleBorder_E9D5FF}" /></DataTrigger><DataTrigger Binding="{Binding Classification}" Value="Farmer"><Setter Property="Background" Value="{DynamicResource ThemeBrush_SuccessSoft_ECFDF3}" /><Setter Property="BorderBrush" Value="{DynamicResource ThemeBrush_SuccessBorder_BBF7D0}" /></DataTrigger><DataTrigger Binding="{Binding Classification}" Value="Vendor"><Setter Property="Background" Value="{DynamicResource ThemeBrush_InfoSoft_EFF6FF}" /><Setter Property="BorderBrush" Value="{DynamicResource ThemeBrush_InfoBorder_BFDBFE}" /></DataTrigger><DataTrigger Binding="{Binding Classification}" Value="Senior Citizen"><Setter Property="Background" Value="{DynamicResource ThemeBrush_WarningSoft_FFF7ED}" /><Setter Property="BorderBrush" Value="{DynamicResource ThemeBrush_WarningBorder_FED7AA}" /></DataTrigger><DataTrigger Binding="{Binding Classification}" Value="Indigenous"><Setter Property="Background" Value="{DynamicResource ThemeBrush_TealSoft_F0FDFA}" /><Setter Property="BorderBrush" Value="{DynamicResource ThemeBrush_TealBorder_99F6E4}" /></DataTrigger><DataTrigger Binding="{Binding Classification}" Value="Indigenous People"><Setter Property="Background" Value="{DynamicResource ThemeBrush_TealSoft_F0FDFA}" /><Setter Property="BorderBrush" Value="{DynamicResource ThemeBrush_TealBorder_99F6E4}" /></DataTrigger></Style.Triggers></Style></Border.Style><StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center"><Ellipse Width="6" Height="6" Margin="0,0,7,0" VerticalAlignment="Center"><Ellipse.Style><Style TargetType="Ellipse"><Setter Property="Fill" Value="{DynamicResource ThemeBrush_SurfaceMuted_94A3B8}" /><Style.Triggers><DataTrigger Binding="{Binding Classification}" Value="PWD"><Setter Property="Fill" Value="{DynamicResource ThemeBrush_Purple_7E22CE}" /></DataTrigger><DataTrigger Binding="{Binding Classification}" Value="Farmer"><Setter Property="Fill" Value="{DynamicResource ThemeBrush_Success_16A34A}" /></DataTrigger><DataTrigger Binding="{Binding Classification}" Value="Vendor"><Setter Property="Fill" Value="{DynamicResource ThemeBrush_Info_2563EB}" /></DataTrigger><DataTrigger Binding="{Binding Classification}" Value="Senior Citizen"><Setter Property="Fill" Value="{DynamicResource ThemeBrush_Danger_C2410C}" /></DataTrigger><DataTrigger Binding="{Binding Classification}" Value="Indigenous"><Setter Property="Fill" Value="{DynamicResource ThemeBrush_Accent_0F766E}" /></DataTrigger><DataTrigger Binding="{Binding Classification}" Value="Indigenous People"><Setter Property="Fill" Value="{DynamicResource ThemeBrush_Accent_0F766E}" /></DataTrigger></Style.Triggers></Style></Ellipse.Style></Ellipse><TextBlock Text="{Binding Classification, TargetNullValue=None, FallbackValue=None}" FontSize="11" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"><TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Foreground" Value="{DynamicResource ThemeBrush_TextSecondary_64748B}" /><Style.Triggers><DataTrigger Binding="{Binding Classification}" Value="PWD"><Setter Property="Foreground" Value="{DynamicResource ThemeBrush_Purple_7E22CE}" /></DataTrigger><DataTrigger Binding="{Binding Classification}" Value="Farmer"><Setter Property="Foreground" Value="{DynamicResource ThemeBrush_Success_15803D}" /></DataTrigger><DataTrigger Binding="{Binding Classification}" Value="Vendor"><Setter Property="Foreground" Value="{DynamicResource ThemeBrush_Info_1D4ED8}" /></DataTrigger><DataTrigger Binding="{Binding Classification}" Value="Senior Citizen"><Setter Property="Foreground" Value="{DynamicResource ThemeBrush_Danger_C2410C}" /></DataTrigger><DataTrigger Binding="{Binding Classification}" Value="Indigenous"><Setter Property="Foreground" Value="{DynamicResource ThemeBrush_Teal_0F766E}" /></DataTrigger><DataTrigger Binding="{Binding Classification}" Value="Indigenous People"><Setter Property="Foreground" Value="{DynamicResource ThemeBrush_Teal_0F766E}" /></DataTrigger></Style.Triggers></Style></TextBlock.Style></TextBlock></StackPanel></Border></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn><DataGridTextColumn Header="Allocated Share" Binding="{Binding ShareText}" Width="1.0*"><DataGridTextColumn.ElementStyle><Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Stretch" /><Setter Property="VerticalAlignment" Value="Center" /><Setter Property="TextAlignment" Value="Center" /><Setter Property="FontWeight" Value="SemiBold" /></Style></DataGridTextColumn.ElementStyle></DataGridTextColumn><DataGridTemplateColumn Header="Release Status" Width="0.95*"><DataGridTemplateColumn.CellTemplate><DataTemplate><Border Height="26" MinWidth="82" Padding="10,0" HorizontalAlignment="Center" VerticalAlignment="Center" BorderThickness="1" CornerRadius="13" SnapsToDevicePixels="True"><Border.Style><Style TargetType="Border"><Setter Property="Background" Value="{DynamicResource ThemeBrush_WarningSoft_FFF7E8}" /><Setter Property="BorderBrush" Value="{DynamicResource ThemeBrush_WarningBorder_FDE7B2}" /><Style.Triggers><DataTrigger Binding="{Binding IsReleased}" Value="True"><Setter Property="Background" Value="{DynamicResource ThemeBrush_SuccessSoft_ECFDF3}" /><Setter Property="BorderBrush" Value="{DynamicResource ThemeBrush_SuccessBorder_BBF7D0}" /></DataTrigger></Style.Triggers></Style></Border.Style><StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center"><Ellipse Width="6" Height="6" Margin="0,0,7,0" VerticalAlignment="Center"><Ellipse.Style><Style TargetType="Ellipse"><Setter Property="Fill" Value="{DynamicResource ThemeBrush_Warning_D97706}" /><Style.Triggers><DataTrigger Binding="{Binding IsReleased}" Value="True"><Setter Property="Fill" Value="{DynamicResource ThemeBrush_Success_16A34A}" /></DataTrigger></Style.Triggers></Style></Ellipse.Style></Ellipse><TextBlock FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center"><TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Text" Value="Waiting" /><Setter Property="Foreground" Value="{DynamicResource ThemeBrush_Warning_B45309}" /><Style.Triggers><DataTrigger Binding="{Binding IsReleased}" Value="True"><Setter Property="Text" Value="Released" /><Setter Property="Foreground" Value="{DynamicResource ThemeBrush_Success_15803D}" /></DataTrigger></Style.Triggers></Style></TextBlock.Style></TextBlock></StackPanel></Border></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn></DataGrid.Columns>
                        </DataGrid>

                        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,14,0,0">
                            <Button Content="Previous" Style="{StaticResource PageButton}" Command="{Binding ReleasePreviousPageCommand}" />

                            <ItemsControl ItemsSource="{Binding ReleasePageNumbers}" Margin="8,0">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>

                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Button Content="{Binding}" Command="{Binding DataContext.ReleaseGoToPageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource PageButton}">
                                                    <Style.Triggers>
                                                        <DataTrigger Value="True">
                                                            <DataTrigger.Binding>
                                                                <MultiBinding Converter="{StaticResource EqualsToBoolConverter}">
                                                                    <Binding />
                                                                    <Binding Path="DataContext.ReleaseCurrentPage" RelativeSource="{RelativeSource AncestorType=UserControl}" />
                                                                </MultiBinding>
                                                            </DataTrigger.Binding>
                                                            <Setter Property="Background" Value="{DynamicResource ThemeBrush_Accent_1F2A44}" />
                                                            <Setter Property="BorderBrush" Value="{DynamicResource ThemeBrush_Accent_1F2A44}" />
                                                            <Setter Property="Foreground" Value="{DynamicResource ThemeBrush_TextPrimary_FFFFFF}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <Button Content="Next" Style="{StaticResource PageButton}" Command="{Binding ReleaseNextPageCommand}" />
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
<!-- CONFIRM RELEASE MODAL -->
        <Grid Panel.ZIndex="920" DataContext="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}" Visibility="{Binding IsConfirmReleaseOpen, Converter={StaticResource BoolToVis}}">
            <Border Background="{DynamicResource ThemeBrush_Overlay_800F172A}" />

            <Border Background="Transparent">
                <Border.InputBindings>
                    <MouseBinding MouseAction="LeftClick" Command="{Binding CloseConfirmReleaseCommand}" />
                </Border.InputBindings>
            </Border>

            <Border Width="980" MaxHeight="720" Background="{DynamicResource ThemeBrush_Surface_FFFFFF}" BorderBrush="{DynamicResource ThemeBrush_Border_F1F5F9}" BorderThickness="1" CornerRadius="24" Padding="24" HorizontalAlignment="Center" VerticalAlignment="Center" Effect="{StaticResource CardShadow}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="18" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="18" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="12" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Border Width="42" Height="42" CornerRadius="13" Background="{DynamicResource ThemeBrush_SuccessSoft_ECFDF3}">
                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="" Foreground="{DynamicResource ThemeBrush_Success_16A34A}" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>

                        <StackPanel Grid.Column="2" VerticalAlignment="Center">
                            <TextBlock Text="Verify Beneficiary Release" Foreground="{DynamicResource ThemeBrush_AccentText_0F172A}" FontSize="20" FontWeight="Bold" />
                            <TextBlock Text="Confirm the identity and allocated share before completing the release." Foreground="{DynamicResource ThemeBrush_TextSecondary_64748B}" FontSize="11" Margin="0,4,0,0" />
                        </StackPanel>

                        <Button Grid.Column="3" Style="{StaticResource CloseButtonStyle}" Command="{Binding CloseConfirmReleaseCommand}">
                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="" FontSize="12" Foreground="{DynamicResource ThemeBrush_TextSecondary_64748B}" />
                        </Button>
                    </Grid>

                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.35*" />
                            <ColumnDefinition Width="18" />
                            <ColumnDefinition Width="0.85*" />
                        </Grid.ColumnDefinitions>

                        <!-- BENEFICIARY PROFILE -->
                        <Border Grid.Column="0" CornerRadius="18" Background="{DynamicResource ThemeBrush_InfoSoft_F8FAFC}" BorderBrush="{DynamicResource ThemeBrush_Border_E7ECF5}" BorderThickness="1" Padding="20">
                            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="92" />
                                            <ColumnDefinition Width="16" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>

                                        <Grid Width="92" Height="92">
                                            <Ellipse Fill="{DynamicResource ThemeBrush_InfoSoft_DDEAFE}" />

                                            <Ellipse>
                                                <Ellipse.Fill>
                                                    <ImageBrush ImageSource="{Binding ConfirmProfileImagePreview}" Stretch="UniformToFill" />
                                                </Ellipse.Fill>
                                                <Ellipse.Style>
                                                    <Style TargetType="Ellipse">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding ConfirmHasProfileImage}" Value="True">
                                                                <Setter Property="Visibility" Value="Visible" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Ellipse.Style>
                                            </Ellipse>

                                            <TextBlock Text="👤" FontSize="38" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding ConfirmHasProfileImage}" Value="True">
                                                                <Setter Property="Visibility" Value="Collapsed" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Grid>

                                        <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding ConfirmName}" Foreground="{DynamicResource ThemeBrush_AccentText_0F172A}" FontSize="21" FontWeight="Bold" TextWrapping="Wrap" />
                                            <TextBlock Text="{Binding ConfirmId, StringFormat=Beneficiary ID: {0}}" Foreground="{DynamicResource ThemeBrush_TextSecondary_64748B}" FontSize="12" Margin="0,6,0,0" />
                                            <Border HorizontalAlignment="Left" Margin="0,10,0,0" Padding="10,5" CornerRadius="10" Background="{DynamicResource ThemeBrush_WarningSoft_FFF7E8}">
                                                <TextBlock Text="Pending verification" Foreground="{DynamicResource ThemeBrush_Warning_D97706}" FontSize="11" FontWeight="SemiBold" />
                                            </Border>
                                        </StackPanel>
                                    </Grid>

                                    <Grid Margin="0,22,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="16" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>

                                        <StackPanel Grid.Row="0" Grid.Column="0">
                                            <TextBlock Text="Civil Registry ID" Style="{StaticResource FieldLabelText}" />
                                            <TextBlock Text="{Binding PendingRelease.CivilRegistryId, TargetNullValue=-}" Foreground="{DynamicResource ThemeBrush_AccentText_0F172A}" FontWeight="SemiBold" />
                                        </StackPanel>

                                        <StackPanel Grid.Row="0" Grid.Column="2">
                                            <TextBlock Text="Gender" Style="{StaticResource FieldLabelText}" />
                                            <TextBlock Text="{Binding PendingRelease.Gender, TargetNullValue=-}" Foreground="{DynamicResource ThemeBrush_AccentText_0F172A}" FontWeight="SemiBold" />
                                        </StackPanel>

                                        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,18,0,0">
                                            <TextBlock Text="Barangay" Style="{StaticResource FieldLabelText}" />
                                            <TextBlock Text="{Binding ConfirmBarangay, TargetNullValue=-}" Foreground="{DynamicResource ThemeBrush_AccentText_0F172A}" FontWeight="SemiBold" />
                                        </StackPanel>

                                        <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,18,0,0">
                                            <TextBlock Text="Classification" Style="{StaticResource FieldLabelText}" />
                                            <TextBlock Text="{Binding ConfirmClassification, TargetNullValue=-}" Foreground="{DynamicResource ThemeBrush_AccentText_0F172A}" FontWeight="SemiBold" />
                                        </StackPanel>

                                        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,18,0,0">
                                            <TextBlock Text="Present Address" Style="{StaticResource FieldLabelText}" />
                                            <TextBlock Text="{Binding PendingRelease.PresentAddress, TargetNullValue=-}" Foreground="{DynamicResource ThemeBrush_AccentText_0F172A}" TextWrapping="Wrap" />
                                        </StackPanel>
                                    </Grid>

                                    <Border Margin="0,22,0,0" Padding="16" CornerRadius="14" Background="{DynamicResource ThemeBrush_Surface_FFFFFF}" BorderBrush="{DynamicResource ThemeBrush_Border_E7ECF5}" BorderThickness="1">
                                        <Grid>
                                            <StackPanel>
                                                <TextBlock Text="SHARE TO RELEASE" Foreground="{DynamicResource ThemeBrush_TextSecondary_64748B}" FontSize="10" FontWeight="Bold" />
                                                <TextBlock Text="{Binding ConfirmShare}" Foreground="{DynamicResource ThemeBrush_AccentText_1F2A44}" FontSize="20" FontWeight="Bold" Margin="0,5,0,0" />
                                            </StackPanel>

                                            <Border HorizontalAlignment="Right" VerticalAlignment="Center" Width="42" Height="42" CornerRadius="13" Background="{DynamicResource ThemeBrush_Accent_EEF2FF}">
                                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="" Foreground="{DynamicResource ThemeBrush_AccentText_1F2A44}" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                            </Border>
                                        </Grid>
                                    </Border>
                                </StackPanel>
                            </ScrollViewer>
                        </Border>

                        <!-- RELEASE HISTORY -->
                        <Border Grid.Column="2" CornerRadius="18" Background="{DynamicResource ThemeBrush_Surface_FFFFFF}" BorderBrush="{DynamicResource ThemeBrush_Border_E7ECF5}" BorderThickness="1" Padding="18">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>

                                <TextBlock Text="Release History" Foreground="{DynamicResource ThemeBrush_AccentText_0F172A}" FontSize="16" FontWeight="Bold" />
                                <TextBlock Grid.Row="1" Text="Previous completed releases" Foreground="{DynamicResource ThemeBrush_TextSecondary_64748B}" FontSize="11" Margin="0,4,0,12" />

                                <Grid Grid.Row="2">
                                    <TextBlock Text="No previous releases found." Foreground="{DynamicResource ThemeBrush_TextSecondary_64748B}" FontStyle="Italic" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Visible" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding HasConfirmReleaseHistory}" Value="True">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>

                                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                        <ItemsControl ItemsSource="{Binding ConfirmReleaseHistory}">
                                            <ItemsControl.Style>
                                                <Style TargetType="ItemsControl">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding HasConfirmReleaseHistory}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </ItemsControl.Style>

                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Margin="0,0,0,10" Padding="12" CornerRadius="12" Background="{DynamicResource ThemeBrush_InfoSoft_F8FAFC}" BorderBrush="{DynamicResource ThemeBrush_Border_E7ECF5}" BorderThickness="1">
                                                        <StackPanel>
                                                            <TextBlock Text="{Binding ReleasedAtText}" Foreground="{DynamicResource ThemeBrush_AccentText_0F172A}" FontWeight="SemiBold" />
                                                            <TextBlock Text="{Binding Description}" Foreground="{DynamicResource ThemeBrush_TextSecondary_64748B}" FontSize="11" Margin="0,4,0,0" TextWrapping="Wrap" />
                                                        </StackPanel>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                </Grid>
                            </Grid>
                        </Border>
                    </Grid>

                    <Grid Grid.Row="4">
                        <Border HorizontalAlignment="Left" VerticalAlignment="Center" Padding="12,8" CornerRadius="11" Background="{DynamicResource ThemeBrush_WarningSoft_FFF7E8}">
                            <TextBlock Text="Verify the beneficiary before releasing the allocation." Foreground="{DynamicResource ThemeBrush_Warning_D97706}" FontSize="11" FontWeight="SemiBold" />
                        </Border>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="Cancel" Width="132" Height="44" Style="{StaticResource LightButton}" Command="{Binding CloseConfirmReleaseCommand}" />

                            <Button Content="Confirm Release" MinWidth="166" Height="44" Margin="12,0,0,0" Style="{StaticResource SuccessButton}" Command="{Binding ConfirmReleaseCommand}" />
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
<!-- LOADING OVERLAY -->
        <Grid Panel.ZIndex="950" Background="{DynamicResource ThemeBrush_Glass_66FFFFFF}" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
            <Border Width="260" Padding="20" CornerRadius="18" Background="{DynamicResource ThemeBrush_Surface_FFFFFF}" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="Loading distribution..." FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource ThemeBrush_AccentText_2E3A59}" HorizontalAlignment="Center" />

                    <ProgressBar IsIndeterminate="True" Height="8" Margin="0,16,0,0" Foreground="{DynamicResource ThemeBrush_AccentText_1F2A44}" Background="{DynamicResource ThemeBrush_InfoSoft_E9EEF7}" />

                    <TextBlock Text="Please wait while data is being fetched." FontSize="12" Foreground="{DynamicResource ThemeBrush_TextSecondary_8A94A6}" Margin="0,12,0,0" TextAlignment="Center" />
                </StackPanel>
            </Border>
        </Grid>

        <!-- REPORT PREVIEW MODAL -->
        <Grid Panel.ZIndex="915" DataContext="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}" Visibility="{Binding IsReportPreviewOpen, Converter={StaticResource BoolToVis}}">
            <Border Background="{DynamicResource ThemeBrush_Overlay_660F172A}" />

            <!-- Click outside -->
            <Border Background="Transparent">
                <Border.InputBindings>
                    <MouseBinding MouseAction="LeftClick" Command="{Binding CloseReportPreviewCommand}" />
                </Border.InputBindings>
            </Border>

            <Border Width="1100" MaxWidth="1100" Height="720" MaxHeight="720" Padding="24" Background="{DynamicResource ThemeBrush_Surface_FFFFFF}" BorderBrush="{DynamicResource ThemeBrush_Border_F1F5F9}" BorderThickness="1" CornerRadius="20" HorizontalAlignment="Center" VerticalAlignment="Center" Effect="{StaticResource CardShadow}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Grid Grid.Row="0" Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel>
                            <TextBlock Text="{Binding ReportPreviewTitle}" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource ThemeBrush_AccentText_0F172A}" />
                            <TextBlock Margin="0,6,0,0" Text="Preview the PDF first, then save it when ready." FontSize="12" Foreground="{DynamicResource ThemeBrush_TextSecondary_64748B}" />
                        </StackPanel>

                        <Button Grid.Column="1" Style="{StaticResource CloseButtonStyle}" Command="{Binding CloseReportPreviewCommand}">
                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="" FontSize="12" Foreground="{DynamicResource ThemeBrush_TextSecondary_64748B}" />
                        </Button>
                    </Grid>

                    <!-- Viewer -->
                    <Border Grid.Row="1" Background="{DynamicResource ThemeBrush_InfoSoft_F8FAFC}" BorderBrush="{DynamicResource ThemeBrush_Border_E7ECF5}" BorderThickness="1" CornerRadius="16" ClipToBounds="True">
                        <Grid>
                            <WebBrowser x:Name="PdfPreviewBrowser" />

                            <Grid Visibility="{Binding HasReportPreview, Converter={StaticResource BoolToVis}}">
                            </Grid>
                        </Grid>
                    </Border>

                    <!-- Footer -->
                    <Grid Grid.Row="2" Margin="0,18,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock VerticalAlignment="Center" Foreground="{DynamicResource ThemeBrush_TextSecondary_64748B}" FontSize="12" Text="Use Save As to store the final PDF." />

                        <Button Grid.Column="1" Margin="0,0,10,0" MinWidth="150" Style="{StaticResource LightButton}" Command="{Binding OpenPreviewExternallyCommand}" Content="Open Externally" />

                        <Button Grid.Column="2" Margin="0,0,10,0" MinWidth="120" Style="{StaticResource LightButton}" Command="{Binding CloseReportPreviewCommand}" Content="Close" />

                        <Button Grid.Column="3" MinWidth="140" Style="{StaticResource PrimaryButton}" Command="{Binding SavePreviewReportCommand}" Content="Save As" />
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>