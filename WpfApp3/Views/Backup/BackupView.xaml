<UserControl x:Class="WpfApp3.Views.Backup.BackupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:WpfApp3.ViewModels.Backup"
             mc:Ignorable="d">

    <UserControl.DataContext>
        <vm:BackupViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="20"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="18" ShadowDepth="0" Opacity="0.08"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1F2A44"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Padding" Value="22,12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="14"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InfoLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <Style x:Key="InfoValueStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F5F7FB" Margin="8">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="8">

                <Border Style="{StaticResource CardBorderStyle}">
                    <StackPanel>
                        <TextBlock Text="Database Backup"
                                   FontSize="28"
                                   FontWeight="Bold"
                                   Foreground="#243B6B"/>

                        <TextBlock Text="Create a backup of the active MySQL database."
                                   Margin="0,8,0,24"
                                   Foreground="#64748B"
                                   FontSize="14"/>

                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                <TextBlock Text="Database" Style="{StaticResource InfoLabelStyle}"/>
                                <TextBlock Text="{Binding DatabaseName}" Style="{StaticResource InfoValueStyle}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Margin="0,0,16,0">
                                <TextBlock Text="Server" Style="{StaticResource InfoLabelStyle}"/>
                                <TextBlock Text="{Binding ServerName}" Style="{StaticResource InfoValueStyle}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Current Role" Style="{StaticResource InfoLabelStyle}"/>
                                <TextBlock Text="{Binding CurrentUserRole}" Style="{StaticResource InfoValueStyle}"/>
                            </StackPanel>
                        </Grid>

                        <Border Background="#EEF2FF"
                                BorderBrush="#C7D2FE"
                                BorderThickness="1"
                                CornerRadius="14"
                                Padding="16"
                                Margin="0,0,0,18">
                            <TextBlock Text="Only Superadmin can generate a database backup."
                                       Foreground="#3730A3"
                                       FontWeight="SemiBold"/>
                        </Border>

                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Button Content="Create Backup"
                                    Width="170"
                                    Height="48"
                                    Command="{Binding CreateBackupCommand}"
                                    Style="{StaticResource PrimaryButtonStyle}"/>

                            <StackPanel Orientation="Horizontal"
                                        Margin="16,0,0,0"
                                        VerticalAlignment="Center"
                                        Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
                                <ProgressBar Width="140"
                                             Height="8"
                                             IsIndeterminate="True"/>
                                <TextBlock Text=" Processing..."
                                           Margin="10,0,0,0"
                                           VerticalAlignment="Center"
                                           Foreground="#64748B"/>
                            </StackPanel>
                        </StackPanel>

                        <Border Margin="0,20,0,0"
                                Padding="14"
                                CornerRadius="12"
                                Background="#F8FAFC"
                                BorderBrush="#E2E8F0"
                                BorderThickness="1">
                            <TextBlock Text="{Binding StatusMessage}"
                                       Foreground="{Binding StatusBrush}"
                                       FontWeight="SemiBold"
                                       TextWrapping="Wrap"/>
                        </Border>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>